fn main() -> u32 {
    let s = "hello, world\n";
    print_uint(s[0] as u32);
    print_uint("Hello, World.\n"[1] as u32);

    let i: u16 = 0;
    for(i=2; s[i] != 0; i+=1) {
        print_uint(s[i] as u32);
    }

    0
}
